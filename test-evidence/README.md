# Test Evidence

This directory holds structured evidence collected during active test cycles: execution
logs, screenshots, and payload captures that back up entries in `bug-reports/` and
`test-cases/`.

It is intentionally empty until a cycle produces real evidence — nothing here is
backfilled or fabricated. The active September 2026 QA cycle is the first expected to
populate it.
